

#======================Main File======================================================
# OpenSearch Configuration
opensearch.host=https://localhost:9200
opensearch.username=admin
opensearch.password=Priyanka@123

# Enable HTTPS and SSL Configuration
quarkus.http.port=8080
quarkus.http.ssl-port=8443
quarkus.http.insecure-requests=disabled
quarkus.http.ssl.certificate.key-store-file-type=JKS
quarkus.http.ssl.certificate.key-store-file=keystore.jks
quarkus.http.ssl.certificate.key-store-password=Priyanka@123


# # Keycloak (OIDC) Configuration
# quarkus.oidc.auth-server-url=http://localhost:8081/realms/myrealm
# quarkus.oidc.client-id=quarkus1-app
# quarkus.oidc.credentials.secret=knmjx6x49f4SeNqkk1Mr0OgvtUnQm5pp
# quarkus.oidc.authentication.user-info-required=true
# quarkus.oidc.application-type=service


# quarkus.oidc.auth-server-url=http:192.168.1.3//:8081/auth/realms/myrealm
# quarkus.oidc.client-id=quarkus1-app
# quarkus.oidc.credentials.secret=knmjx6x49f4SeNqkk1Mr0OgvtUnQm5pp
# quarkus.oidc.authentication.user-info-required=true
# quarkus.oidc.application-type=service

#=============Not Used========================================
# # OIDC Endpoints
# #quarkus.oidc.token.issuer=http://localhost:8081/realms/myrealm
# authorization_endpoint=http://localhost:8080/realms/myrealm/protocol/openid-connect/auth
# token_endpoint=http://localhost:8080/realms/myrealm/protocol/openid-connect/token
# userinfo_endpoint=http://localhost:8080/realms/myrealm/protocol/openid-connect/userinfo


# # OIDC Token Issuer URL (Uncommented and Modified)
# quarkus.oidc.token.issuer=http://localhost:8080/realms/myrealm/protocol/openid-connect/token

# # OIDC TLS Configuration (disabled verification for development purposes)
# quarkus.oidc.tls.verification=none

# # Log Configuration for OIDC debugging
# quarkus.log.category."io.quarkus.oidc".level=DEBUG

#===============================================================================================================

